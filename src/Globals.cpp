#include "Globals.h"

// SemaphoreHandle_t canvasSemaphore;
// M5GFX display;              // Definição da variável global
// M5Canvas canvas(&display);  // Inicialize canvas com display
// M5Canvas titleBar(&display);
// M5Canvas center(&display);
